public class MatrixMultiply {

    /* 
     * Given a sequence of matrices, find the most efficient way to multiply these matrices.
     * The problem is not actually to perform the multiplications, but merely to decide in which order
     * to perform the multiplications. i.e count the minimum number of multiplications.
     * 
     * EXAMPLES:
     * 
     * 1) M1(10x30), M2(30x5), M3(5x60)
     * 
     * i) (M1*M2)*M3 = 10*30*5 + 10*5*60 = 1500 + 3000 = 4500
     * ii) M1*(M2*M3) = 30*5*60 + 10*30*60 = 9000 + 18000 = 27000
     * 
    */

    public static matrixMultiply(int start, int end) {
        for (int i = s i <= e; i++) {
            
        }
    }

    public static void main(String[] args) {
        
    }
}
